#!/usr/bin/env bash
# merge-wiki-to-adr.sh — N wiki → 기존 ADR 의 sources 에 추가 합치기.
# Usage: merge-wiki-to-adr.sh <wiki-file> <adr-file>
#
# placeholder. ADR atomic 단위 — *기존 ADR 갱신* 보다 *새 ADR + supersedes* 패턴 자연.
# 운영 시 사례 발견하면 superseded 흐름 박은 후 보강.

echo "[$(basename "$0")] not yet implemented. ADR supersedes 패턴 vs 갱신 결정 후 보강." >&2
exit 99
