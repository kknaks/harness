#!/usr/bin/env bash
# merge-inbox-to-idea.sh — N inbox (메타 정책 분류된) → 기존 idea 의 sources 에 추가 합치기.
# Usage: merge-inbox-to-idea.sh <inbox-file> <idea-file>
#
# placeholder. 콘텐츠 inbox raw 를 메타 idea 에 합치는 패턴 (cross-layer) — ADR-0001 §레이어 간
# 위키링크 참조 금지 와 미세 충돌 가능. 운영 시 사례 발견하면 별도 ADR 에서 예외 규약 박은 후 보강.

echo "[$(basename "$0")] not yet implemented. cross-layer 병합 정책 결정 후 본문 박음 (ADR-0001 §레이어 간 참조 예외)." >&2
exit 99
