#!/usr/bin/env bash
# merge-inbox-to-sources.sh — N inbox → 기존 sources 의 sources 에 추가 합치기.
# Usage: merge-inbox-to-sources.sh <inbox-file> <sources-file>
#
# placeholder. ADR-0003 R2 (sources 불변) 와 충돌 가능 — *기존 sources 갱신* 자체가 R2 위반.
# 운영 시 *기존 sources 에 추가 raw 합치기 = 정정* 케이스 발견하면 R2 와의 관계 박은 후 보강.

echo "[$(basename "$0")] not yet implemented. ADR-0003 R2 (sources 불변) 와 충돌 정합 박은 후 보강." >&2
exit 99
