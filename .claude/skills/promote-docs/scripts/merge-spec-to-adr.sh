#!/usr/bin/env bash
# merge-spec-to-adr.sh — N spec → 기존 ADR 의 sources 에 추가 합치기 (병합).
# Usage: merge-spec-to-adr.sh <spec-file> <adr-file>
#
# placeholder. 메타 단계 frontmatter 는 결정됨 (ADR-0004) 이지만 *기존 ADR 갱신 vs supersedes 새 ADR*
# 패턴 결정이 미박. 운영 시 N spec → 1 ADR 사례 발견 시 본문 보강.
# (현재 idea→spec 병합은 merge.sh, 다른 단계 병합은 6 placeholder sh 로 틀만.)

echo "[$(basename "$0")] not yet implemented. ADR-0012 §운영 후 결정 — 콘텐츠 가동하며 N→1 빈도 측정 후 본문 박음." >&2
exit 99
